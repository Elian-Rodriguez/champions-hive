[pytest]
testpaths = tests
# Los avisos de deprecación de Pydantic v2 sobre `class Config` inundan la
# salida y no dicen nada sobre el resultado de las pruebas.
filterwarnings =
    ignore::DeprecationWarning
